{% extends 'base.html.twig' %}

{% block body %}
<h1>Producto {{ producto.nombre }}</h1>
<p>Precio: {{ producto.precio }}</p>
<a href="{{ path('producto_index') }}" class="btn btn-secondary">Volver</a>
{% endblock %}
